name: bugbot-cleanup

on:
  workflow_dispatch:
    inputs:
      branch:
        required: true
        type: string

jobs:
  cleanup:
    uses: BenEliyahu/claude-bugbot/.github/workflows/reusable-cleanup.yml@main
    permissions:
      contents: write
    with:
      branch: ${{ inputs.branch }}
